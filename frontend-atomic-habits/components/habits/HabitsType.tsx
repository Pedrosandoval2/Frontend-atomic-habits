import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

export const HabitsType = () => {
    return (
        <View>
            <Text style={styles.text}>HabitsType</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    text: { color: 'white' }
});
